# efood React (Vite)

Projeto inicial usando Vite + React, Styled Components e React Router.

Instalação:

```bash
npm install
npm run dev
```

Observações:
- Layout de exemplo baseado no Figma fornecido; ajuste estilos conforme necessário.
- Rotas: `/` (home) e `/restaurant/:id` (página do restaurante).
